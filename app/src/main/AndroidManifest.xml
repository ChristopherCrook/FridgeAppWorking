<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myfridge">

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_chris_class_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_chris_class_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFridge">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddGroceryActivity" />
        <activity android:name=".AddAlarmActivity"/>
        <activity android:name=".FridgeActivity"/>
        <activity android:name=".ViewItemActivity" />
        <receiver
            android:name=".NotificationReceiver"
            android:enabled="true" />
    </application>

</manifest>